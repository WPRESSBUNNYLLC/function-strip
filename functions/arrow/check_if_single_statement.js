var first_statement_descriptor = [];
var build_string_ = '';
var line_number_ = 0;
var data_index_ = 0;

function check_if_a_single_statement(data_index, line_number, build_string) { 
 build_string = build_string;
 line_number = line_number;
 data_index = data_index;
 recurse(data_index_);
}

function recurse(data_index_) { //only in the case of ( ... i need to build an array and iterate over the operations or a few to determine if a single statement
 
}
