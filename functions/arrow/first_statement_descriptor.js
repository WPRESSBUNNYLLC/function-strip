
//if a new line comes in an arrow function without an opening bracket, return the first statement.

var first_statement_descriptor = [];
var build_string_ = '';
var line_number_ = 0;
var data_index_ = 0;

function finish_first_statement(data_index, line_number, build_string) { 
 build_string = build_string;
 line_number = line_number;
 data_index = data_index;
 initial_build(data_index_);
}

function initial_build(data_index_) { 
 
}

function after_initial() { 

}