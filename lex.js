var update_function_and_update_data = require('../data');
var data = '';
var lex = {}; //this is not a lex
var current_file = '';
var current_name = '';
var current_type = '';
var current_value = '';
var extended = false;
var data_index_ = 0;
function update_lex(file, name, type, extended, data_index) { 
//get equals move back and get value
//lex[file][name] = {t: type, v: [value]} if an extended value then append the extension + to the last index
}
function update_value() { 

}
module.exports = update_lex;
//beginning will be last value of lex for the current 